# FastAPI 및 서버
fastapi
uvicorn[standard]
python-multipart

# 데이터베이스
mysql-connector-python


# AWS S3
boto3

# 비디오 처리
ffmpeg-python==0.2.0
static-ffmpeg==2.5

# AI 하이라이트 자동 생성 (비전 기반 - YOLOv8 + OpenCV)
# GPU(CUDA) 사용 가능 시 자동 사용, 불가능하면 CPU로 자동 전환됨
ultralytics>=8.0
opencv-python-headless>=4.8
numpy>=1.24.0
scipy>=1.11.0

# 유틸리티
python-dotenv

# 인증
PyJWT==2.8.0
google-auth==2.29.0
httpx==0.27.0

# 로깅
loguru==0.7.2

# CORS
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
