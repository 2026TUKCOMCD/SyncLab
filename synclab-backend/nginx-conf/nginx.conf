worker_processes  1;

events {
    worker_connections  1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    # 로그
    access_log  logs/access.log;
    error_log   logs/error.log info;

    server {
        listen       80;
        server_name  localhost;

        # 대용량 업로드
        client_max_body_size 10G;
        client_body_timeout 300s;

        # ============================================
        # 프록시/업로드 관련 → makeproxyvideo.py (포트 8001)
        # ============================================
        location /api/video/upload/ {
            proxy_pass http://127.0.0.1:8001;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            
            # 타임아웃 (프록시 생성 시간 고려)
            proxy_connect_timeout 600s;
            proxy_send_timeout 600s;
            proxy_read_timeout 600s;
        }
        
        location /api/video/proxy/ {
            proxy_pass http://127.0.0.1:8001;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # FastAPI (모든 API)
        location /api/ {
            proxy_pass http://127.0.0.1:3000;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # 타임아웃
            proxy_connect_timeout 300s;
            proxy_send_timeout 300s;
            proxy_read_timeout 300s;
        }

        # 정적 파일 (원본)
        location /static/original/ {
            alias C:/Users/paul2/Desktop/synclab-fastapi/uploads/original/;
            autoindex on;
            add_header Access-Control-Allow-Origin *;
        }

        # 정적 파일 (프록시)
        location /static/proxy/ {
            alias C:/Users/paul2/Desktop/synclab-fastapi/uploads/proxy/;
            autoindex on;
            add_header Access-Control-Allow-Origin *;
        }

        # 프론트엔드 (나중에)
        location / {
            proxy_pass http://127.0.0.1:3000;
        }

        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }
    }
}