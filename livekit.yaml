port: 7880
rtc:
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: false
  # 모바일 기기가 WebRTC 미디어(UDP)로 연결할 PC의 LAN IP
  # Docker 내부 IP(172.x.x.x) 대신 실제 WiFi IP를 ICE 후보로 광고
  node_ip: 10.46.15.64

keys:
  synclab-key: synclab-secret-minimum-32-chars-long!!

logging:
  level: info
